<!-- Generated by GitHub Copilot agent scan on 2025-12-03 -->
# Copilot / AI Agent Instructions — mindful-athlete

Summary
- This repository contains two separate Expo-managed React Native apps and a placeholder Python backend:
  - `frontend/` — Expo + TypeScript app (uses `nativewind`, `tailwindcss` in deps)
  - `mobile-app/` — second Expo + TypeScript app (nearly identical structure)
  - `backend/` — currently contains a Python virtualenv (`.venv`) and an empty `frontend/` folder (no runnable server entrypoint found)

Quick goals for suggestions
- Make suggestions that are safe to run inside each Expo app folder (do not assume a monorepo package manager).
- Prefer minimal, incremental changes (small PRs) that preserve `App.tsx`/`index.ts` entry structure.
- Do not delete or refactor nested duplicate folders (`frontend/frontend`, `mobile-app/mobile-app`) without explicit confirmation.

Key commands (examples)
- Start the frontend app:
  - `cd frontend` and `npm install` (or `npm ci`) then `npm run start`
  - `npm run android` / `npm run ios` / `npm run web` (uses Expo CLI)
- Start the mobile app:
  - `cd mobile-app` and `npm install` then `npm run start`
- TypeScript checks (per-app):
  - `cd frontend` && `npx tsc --noEmit` (same for `mobile-app`)
- Python backend (no run script discovered):
  - Activate virtualenv: `cd backend` && `.\.venv\Scripts\Activate.ps1` (PowerShell)
  - A `requirements.txt` file was added to `backend/` (placeholder). An empty `requirements.txt` previously existed in `mobile-app/` and has been relocated — prefer editing `backend/requirements.txt` for Python deps.

Architecture notes (what to know before editing)
- Each app is a standalone Expo-managed project with an `App.tsx` root and `index.ts` that calls `registerRootComponent(App)`.
- The apps use TypeScript and extend `expo/tsconfig.base` with `strict: true` in `tsconfig.json` — keep generated types correct and avoid bypassing `any` unless necessary.
- `frontend/package.json` includes `nativewind` and `tailwindcss`. If altering UI, search for a `tailwind.config.js` or build steps before changing Tailwind classes (none found during scan).
- No centralized shared package directory or workspace was detected — treat `frontend/` and `mobile-app/` independently unless a maintainer asks otherwise.

Conventions and patterns to follow
- Entry points: keep `App.tsx` minimal and export default a React functional component.
- Styling: Tailwind + NativeWind conventions may be used in `frontend/` — prefer class-based styling consistent with existing files.
- Scripts: use the `npm` scripts defined in each `package.json` rather than inventing new global scripts.
- Type safety: honor `tsconfig.json` (`strict: true`); when recommending code, prefer explicit types and small helper functions.

Integration and external dependencies
- Mobile apps are Expo-managed; they expect Expo CLI available in developer environments.
- No remote backend endpoints or environment files discovered — do not hardcode API URLs. If suggesting an API client, add configuration points (e.g., `ENV` or `config.ts`) and document where to set URLs.

Files to inspect when making changes
- `frontend/package.json`, `frontend/App.tsx`, `frontend/index.ts`, `frontend/tsconfig.json`
- `mobile-app/package.json`, `mobile-app/App.tsx`, `mobile-app/index.ts`, `mobile-app/tsconfig.json`
- `mobile-app/package.json`, `mobile-app/App.tsx`, `mobile-app/index.ts`, `mobile-app/tsconfig.json`
- Note: the nested `mobile-app/mobile-app/package.json` contained a more complete dependency list (Expo modules + `nativewind`). The top-level `mobile-app/package.json` has been consolidated with those dependencies — run `cd mobile-app && npm install` after pulling these changes.
  - Note: the nested `mobile-app/mobile-app/package.json` contained a more complete dependency list (Expo modules + `nativewind`). The top-level `mobile-app/package.json` has been consolidated with those dependencies — run `cd mobile-app && npm install` after pulling these changes.

Dependency alignment
- Aligned `tailwindcss` versions across apps to `^3.4.18` to satisfy `nativewind` peer-dependency expectations. If you intentionally need Tailwind v4 features, evaluate `nativewind` compatibility before bumping.
  - Note: the nested `mobile-app/mobile-app/package.json` contained a more complete dependency list (Expo modules + `nativewind`). The top-level `mobile-app/package.json` has been consolidated with those dependencies — run `cd mobile-app && npm install` after pulling these changes.

Repository maintenance performed by agent
- Safe merge: important app files from `mobile-app/mobile-app/` were reviewed and the top-level `mobile-app/` now contains the canonical app files. The nested folder was intentionally left in place as an archive.
- Backups: backups of top-level files were created in `mobile-app/` as `.bak` files (`App.tsx.bak`, `index.ts.bak`, `package.json.bak`, `tsconfig.json.bak`).
- Archive: the nested `mobile-app/mobile-app` folder has been backed up to `mobile-app/mobile-app-backup.zip` and renamed to `mobile-app/_archive_mobile-app`.
- Why: this is non-destructive and gives maintainers a clear rollback path. If you want the archive renamed or deleted, I can perform that next (I'll create an additional zip backup first).
- `backend/.venv/` and `backend/` (confirm before modifying)

What I did when generating this file
- Searched for existing agent docs and READMEs — none were found to merge.
- Opened `package.json`, `App.tsx`, `index.ts`, and `tsconfig.json` for the two app folders and the backend folder.

If something is unclear
- Ask whether `frontend` and `mobile-app` are intentionally separate apps or if one should be canonical.
- Confirm whether `backend/` is still under development and whether there is a server entrypoint or `requirements.txt` to add.

Next steps I can do
- Add CI job templates to run per-app `npx tsc --noEmit` and `npm run build` if requested.
- Normalize duplicated folders after confirmation from maintainers.
- A GitHub Actions CI workflow was added at `./.github/workflows/ci.yml` to run TypeScript checks in both `frontend` and `mobile-app`.

-- End of generated guidance
